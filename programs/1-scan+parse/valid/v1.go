package main

func main() {
	a := (a).test
	switch os := runtime.GOOS; os {
	case 1:
		// break; // Error: break can only occur in a loop.
	case "linux":
	default:
		// freebsd, openbsd,
		// plan9, windows...
	}
	b := (a).test.test.test
}
