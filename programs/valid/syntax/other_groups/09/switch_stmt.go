// Various allowed switch statements
package swith

func main() {
	switch i++; a {
	}

	switch exp {
	case condition:

	case otherCondition:
	default:
		// break; // Error: break must only occur in a loop.
	}

	// single line
	switch a { case condition: }
}
